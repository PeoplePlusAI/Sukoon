name: Sukoon CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.10]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov black flake8

    - name: Code quality checks
      run: |
        black --check src/
        flake8 src/ --count --max-complexity=10 --max-line-length=127 --statistics

    - name: Run tests with coverage
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        LANGCHAIN_API_KEY: ${{ secrets.LANGCHAIN_API_KEY }}
      run: |
        pytest tests/ --cov=src/ --cov-report=xml -v

    - name: Upload coverage report
      uses: actions/upload-artifact@v3
      with:
        name: coverage-report
        path: coverage.xml
        retention-days: 7

  build-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/frontend

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: src/frontend/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Build frontend
      run: npm run build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: frontend-build
        path: src/frontend/build
        retention-days: 7

  build-backend:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Create production build
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        python setup.py bdist_wheel

    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: backend-build
        path: dist/
        retention-days: 7


# with improved testing
# name: Sukoon CI/CD Pipeline

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# jobs:
#   backend-tests:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: [3.10]

#     steps:
#     - uses: actions/checkout@v4
    
#     - name: Set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v4
#       with:
#         python-version: ${{ matrix.python-version }}
#         cache: 'pip'
    
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#         pip install pytest pytest-cov flake8 black mypy
    
#     - name: Code formatting check with Black
#       run: |
#         black --check src/backend

#     - name: Lint with flake8
#       run: |
#         flake8 src/backend --count --max-complexity=10 --max-line-length=127 --statistics

#     - name: Type checking with mypy
#       run: |
#         mypy src/backend

#     - name: Run backend tests with coverage
#       env:
#         OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#         LANGCHAIN_API_KEY: ${{ secrets.LANGCHAIN_API_KEY }}
#       run: |
#         pytest tests/unit tests/integration --cov=src/backend --cov-report=xml

#     - name: Upload coverage to Codecov
#       uses: codecov/codecov-action@v3
#       with:
#         file: ./coverage.xml
#         fail_ci_if_error: true

#   frontend-tests:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: src/frontend

#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'
#         cache: 'npm'
#         cache-dependency-path: src/frontend/package-lock.json

#     - name: Install dependencies
#       run: npm ci

#     - name: Lint frontend
#       run: |
#         npm run lint
#         npm run prettier:check

#     - name: Run frontend tests
#       run: npm test -- --coverage

#     - name: Build frontend
#       run: npm run build

#   security-scan:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v4

#     - name: Run Snyk to check for vulnerabilities
#       uses: snyk/actions/python@master
#       env:
#         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
#       with:
#         args: --severity-threshold=high

#     - name: Run OWASP Dependency Check
#       uses: dependency-check/Dependency-Check_Action@main
#       with:
#         project: 'Sukoon'
#         path: '.'
#         format: 'HTML'

#   docker:
#     needs: [backend-tests, frontend-tests, security-scan]
#     if: github.ref == 'refs/heads/main'
#     runs-on: ubuntu-latest
    
#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up QEMU
#       uses: docker/setup-qemu-action@v3

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     - name: Build and push Backend
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         file: ./Dockerfile
#         push: true
#         tags: |
#           ${{ secrets.DOCKERHUB_USERNAME }}/sukoon-backend:latest
#           ${{ secrets.DOCKERHUB_USERNAME }}/sukoon-backend:${{ github.sha }}
#         cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sukoon-backend:buildcache
#         cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sukoon-backend:buildcache,mode=max

#   deploy:
#     needs: docker
#     if: github.ref == 'refs/heads/main'
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Deploy to production
#       uses: appleboy/ssh-action@master
#       with:
#         host: ${{ secrets.DEPLOY_HOST }}
#         username: ${{ secrets.DEPLOY_USER }}
#         key: ${{ secrets.DEPLOY_KEY }}
#         script: |
#           cd /opt/sukoon
#           docker-compose pull
#           docker-compose up -d
#           docker system prune -f

#     - name: Create Release
#       uses: actions/create-release@v1
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#       with:
#         tag_name: v${{ github.run_number }}
#         release_name: Release v${{ github.run_number }}
#         body: |
#           Release v${{ github.run_number }}
#           Commit: ${{ github.sha }}
#         draft: false
#         prerelease: false

#     - name: Notify on Slack
#       uses: 8398a7/action-slack@v3
#       with:
#         status: ${{ job.status }}
#         fields: repo,message,commit,author,action,eventName,ref,workflow,job,took
#       env:
#         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#       if: always()